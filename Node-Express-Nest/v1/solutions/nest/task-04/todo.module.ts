import { Module } from "@nestjs/common";
import { TodoController } from "./todo.controller";
import { TodoService } from "./todo.service";

/**
 * TodoModule
 *
 * TODO: register `TodoController` in `controllers` and `TodoService`
 * in `providers`.
 */
@Module({
  controllers: [],
  providers: [],
})
export class TodoModule {}
